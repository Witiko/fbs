(v)The notifier is running. Call via (``^notify^"STATE"^).
(/ Where STATE is one of the blocking fbs commands. (;)
(v)(:notify)
  Waiting for `getCurrName()` to become `eventData`.
  (```eventData```)(notify)
